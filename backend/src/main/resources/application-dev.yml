spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_interview?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    com.a05.aiinterview: DEBUG

app:
  cors:
    # 开发环境临时放开公网来源，便于 cpolar 联调；测试结束后应收紧。
    allowed-origin-patterns: "*"

admin:
  security:
    accounts:
      - username: super-admin
        passwordHash: $2a$10$CdzB/ZLFb67rErOfqFQExe2w3jw5trvg6cFRPbVijGol3yQzINcda
        displayName: 超级管理员
