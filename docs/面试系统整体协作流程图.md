# 面试系统整体协作流程图

## 1. 系统整体架构与数据流向图

```mermaid
flowchart TB
    subgraph 前端层
        User[用户]
        FE_Config[面试配置<br>InterviewPage]
        FE_Interview[面试界面<br>QuestionStream]
        FE_Result[结果展示<br>InterviewResultPage]
    end

    subgraph API层
        IC[InterviewController]
    end

    subgraph 业务服务层
        IS[InterviewService]
        ASS[AnswerSubmitService]
        QSS[QuestionStreamService]
        IRS[InterviewReportService]
    end

    subgraph 引擎层
        POS[PlannerOrchestrationService]
        DEPB[DecisionExecutionPlanBuilder]
        DRO[DecisionRepairOrchestrator]
        SFB[SystemFallbackPlanBuilder]
        ASA[AvailableStrategyAssembler]
        SLR[StateLedgerReducer]
        FQGS[FirstQuestionGenerationService]
        RGS[ReportGenerationService]
    end

    subgraph AI能力层
        AC[AiClient]
        subgraph AI Agents
            PlannerAgent[Planner Agent<br>考纲生成]
            EvalAgent[Evaluation Agent<br>答案评估]
            QGenAgent[QuestionGen Agent<br>题目生成]
            ReportAgent[Report Agent<br>报告生成]
        end
    end

    subgraph RAG层
        RPC[RagPlanCompiler]
        RRS[RagRetrievalService]
        VS[(向量数据库)]
        RR[RerankService<br>重排序]
    end

    subgraph 数据持久层
        ISM[InterviewSessionMapper]
        IQM[InterviewQuestionMapper]
        IAM[InterviewAttemptMapper]
        IRM[InterviewReportMapper]
        Redis[(Redis<br>SSE缓存)]
        MySQL[(MySQL<br>业务数据)]
    end

    User --> FE_Config
    FE_Config --> IC
    IC --> IS
    IS --> POS
    POS --> PlannerAgent
    PlannerAgent --> AC
    POS --> SLR
    SLR --> ISM
    ISM --> MySQL
    POS --> FQGS
    FQGS --> QGenAgent
    QGenAgent --> AC
    FQGS --> IQM
    IQM --> MySQL
    IS --> FE_Interview

    User --> FE_Interview
    FE_Interview --> IC
    IC --> ASS
    ASS --> ASA
    ASA --> 策略筛选
    ASS --> EvalAgent
    EvalAgent --> AC
    ASS --> DEPB
    ASS --> DRO
    ASS --> SFB
    ASS --> SLR
    SLR --> 状态更新
    ASS --> IAM
    IAM --> MySQL
    ASS --> QSS
    QSS --> RPC
    RPC --> RRS
    RRS --> VS
    RRS --> RR
    RR --> RRS
    RRS --> QGenAgent
    QGenAgent --> AC
    QSS --> Redis
    QSS --> IQM
    IQM --> MySQL
    QSS --> FE_Interview

    User --> FE_Interview
    FE_Interview --> IC
    IC --> IRS
    IRS --> RGS
    RGS --> ReportAgent
    ReportAgent --> AC
    RGS --> IRM
    IRM --> MySQL
    IRS --> FE_Result

    策略筛选 -.-> ASA
    状态更新 -.-> SLR
```

## 2. 完整的面试链路时序图（含Agent协作、RAG调用、状态更新、策略筛选）

```mermaid
sequenceDiagram
    autonumber
    participant User as 用户
    participant FE as 前端
    participant IC as InterviewController
    participant IS as InterviewService
    participant POS as PlannerOrchestrationService
    participant AC as AiClient
    participant PlannerAgent as Planner Agent
    participant SLIS as StateLedgerInitService
    participant FQGS as FirstQuestionGenerationService
    participant QGenAgent as QuestionGen Agent
    participant ISM as InterviewSessionMapper
    participant IQM as InterviewQuestionMapper
    participant MySQL as MySQL
    participant Redis as Redis
    participant ASS as AnswerSubmitService
    participant ASA as AvailableStrategyAssembler
    participant EvalAgent as Evaluation Agent
    participant DEPB as DecisionExecutionPlanBuilder
    participant DRO as DecisionRepairOrchestrator
    participant SFB as SystemFallbackPlanBuilder
    participant SLR as StateLedgerReducer
    participant IAM as InterviewAttemptMapper
    participant QSS as QuestionStreamService
    participant RPC as RagPlanCompiler
    participant RRS as RagRetrievalService
    participant VS as 向量数据库
    participant RR as RerankService
    participant IRS as InterviewReportService
    participant RGS as ReportGenerationService
    participant ReportAgent as Report Agent
    participant IRM as InterviewReportMapper

    Note over User,MySQL: === 阶段1：面试初始化 ===
    
    User->>FE: 填写面试配置
    FE->>IC: POST /interviews
    IC->>IS: createInterview()
    
    IS->>ISM: 保存InterviewSession
    ISM->>MySQL: INSERT
    
    IS->>POS: runAsync(sessionId)
    
    POS->>POS: 获取简历、岗位信息
    POS->>AC: callPlanner()
    AC->>PlannerAgent: 生成考纲
    PlannerAgent-->>AC: PlannerOutput
    AC-->>POS: PlannerOutput
    
    POS->>POS: 领域归一化、经验合并、历史去重
    POS->>SLIS: initLedger()
    SLIS-->>POS: stateLedger
    
    POS->>FQGS: generateAndSave()
    FQGS->>AC: callQuestionGeneration()
    AC->>QGenAgent: 生成首题
    QGenAgent-->>AC: 首题
    AC-->>FQGS: 首题
    
    FQGS->>IQM: 保存InterviewQuestion
    IQM->>MySQL: INSERT
    
    POS->>ISM: 更新状态为in_progress
    ISM->>MySQL: UPDATE
    
    POS-->>IS: 初始化完成
    IS-->>IC: CreateInterviewResponse
    IC-->>FE: sessionId
    
    FE->>FE: 轮询会话状态
    FE->>IC: GET /interviews/{sessionId}
    IC->>IS: getSessionDetail()
    IS->>ISM: SELECT
    ISM-->>IS: InterviewSession
    IS-->>IC: InterviewDetailDto
    IC-->>FE: status=in_progress

    Note over User,VS: === 阶段2：答题循环（核心协作流程） ===
    
    FE->>FE: SSE连接接收首题
    FE->>IC: GET /interviews/{sessionId}/questions/stream
    IC->>QSS: streamQuestion()
    QSS->>Redis: 检查缓存
    Redis-->>QSS: 无缓存
    QSS->>QSS: 验证权限
    QSS->>FE: SSE流开始
    QSS->>QSS: 流式推送首题
    QSS-->>FE: SSE delta事件
    QSS-->>FE: SSE done事件

    User->>FE: 回答问题
    
    FE->>IC: POST /interviews/{sessionId}/attempts
    IC->>ASS: submitAnswer()
    
    ASS->>IAM: 幂等性检查
    IAM->>MySQL: SELECT
    IAM-->>ASS: 无重复
    
    ASS->>ASS: 验证会话和题目
    ASS->>ASA: assemble() 策略筛选
    
    Note over ASS,ASA: 策略筛选输入:
    Note over ASS,ASA: • 当前题目类型
    Note over ASS,ASA: • 是否有项目
    Note over ASS,ASA: • 剩余知识域
    Note over ASS,ASA: • 配额状态
    Note over ASS,ASA: • 经验级别
    
    ASA->>ASA: 筛选可用策略
    Note over ASA,ASA: • INTRO (自我介绍)
    Note over ASA,ASA: • PRINCIPLE (原则题)
    Note over ASA,ASA: • FOLLOWUP (追问)
    Note over ASA,ASA: • PROJECT_DEEP_DIVE (项目深挖)
    Note over ASA,ASA: • DOMAIN_SWITCH (领域跳转)
    Note over ASA,ASA: • WRAPUP (结束)
    
    ASA-->>ASS: 可用策略列表
    
    ASS->>ASS: 构建EvaluationDecisionInput
    Note over ASS,ASS: 包含:
    Note over ASS,ASS: • 会话信息
    Note over ASS,ASS: • 当前题目
    Note over ASS,ASS: • 历史题目和回答
    Note over ASS,ASS: • 用户答案
    Note over ASS,ASS: • 剩余知识域
    Note over ASS,ASS: • 配额状态
    Note over ASS,ASS: • 可用策略
    
    ASS->>AC: callEvaluationDecision()
    AC->>EvalAgent: 评估答案
    EvalAgent-->>AC: EvaluationDecisionOutput
    AC-->>ASS: EvaluationDecisionOutput
    
    ASS->>DEPB: build() 构建执行计划
    DEPB->>DEPB: 验证决策有效性
    
    alt 决策有效
        DEPB-->>ASS: 有效决策
    else 决策无效
        DEPB-->>ASS: 无效决策
        ASS->>DRO: repair() 修复决策
        alt 修复成功
            DRO-->>ASS: 修复后决策
        else 修复失败
            DRO-->>ASS: 修复失败
            ASS->>SFB: 构建兜底决策
            SFB-->>ASS: 兜底决策
        end
    end
    
    ASS->>SLR: reduce() 状态更新
    
    Note over ASS,SLR: 状态更新:
    Note over ASS,SLR: • 配额管理 - 更新各类题目计数
    Note over ASS,SLR: • 知识点覆盖 - 记录已覆盖的知识点
    Note over ASS,SLR: • 活动项跟踪 - 跟踪当前项目/实习
    Note over ASS,SLR: • 轮换索引 - 用于兜底策略轮换
    
    SLR->>SLR: 更新state_ledger_json
    SLR-->>ASS: 更新后的状态账本
    
    ASS->>IAM: 保存InterviewAttempt
    IAM->>MySQL: INSERT
    
    alt 决策=WRAPUP
        ASS->>RGS: generateAsync()
        ASS-->>IC: SubmitAttemptResponse(decision=wrapup)
    else 决策=CONTINUE
        ASS-->>IC: SubmitAttemptResponse(decision=continue)
    end
    
    IC-->>FE: response

    alt 继续面试
        FE->>IC: GET /interviews/{sessionId}/questions/stream
        IC->>QSS: streamQuestion()
        
        QSS->>Redis: 检查缓存
        Redis-->>QSS: 无缓存
        
        QSS->>QSS: 验证权限
        QSS->>QSS: 提取下一题计划
        
        QSS->>RPC: compile(decisionPlan) RAG检索计划
        RPC-->>QSS: RagRetrievalRequest
        
        QSS->>RRS: retrieve(request) RAG检索
        RRS->>VS: 向量检索
        VS-->>RRS: 初始检索结果
        RRS->>RR: 重排序
        RR-->>RRS: 重排序结果
        RRS->>RRS: 构建RAG上下文
        RRS->>RRS: 生成总结
        RRS->>RRS: 提取后续候选
        RRS-->>QSS: RagContext
        
        QSS->>QSS: 构建题目生成输入
        QSS->>Redis: 锁定缓存
        QSS->>Redis: 清除旧缓存
        
        QSS->>QSS: 后台启动生成
        
        QSS->>AC: callQuestionGenerationStream()
        AC->>QGenAgent: 流式生成题目
        QGenAgent-->>AC: token流
        AC-->>QSS: token流
        
        loop 每个token
            QSS->>QSS: 追加到完整文本
            QSS->>QSS: 句子缓冲
            QSS->>Redis: 推送到chunks
            QSS->>QSS: chunk索引+1
            
            alt 句子闭合
                QSS->>QSS: 触发分段TTS
                QSS->>Redis: 发送tts_ready
            end
            
            QSS-->>FE: SSE delta事件
        end
        
        QSS->>QSS: 刷新剩余句子
        QSS->>IQM: 保存InterviewQuestion
        IQM->>MySQL: INSERT
        
        QSS->>SLR: reduce() 更新状态账本
        SLR->>SLR: 更新配额、覆盖知识点等
        SLR-->>QSS: 更新后的状态
        
        QSS->>ISM: 更新session.current_question_no
        ISM->>MySQL: UPDATE
        
        QSS->>Redis: 缓存done状态
        QSS-->>FE: SSE done事件
        
        FE->>User: 展示下一题
    end

    Note over User,IRM: === 阶段3：报告生成 ===
    
    User->>FE: 结束面试
    FE->>IC: POST /interviews/{sessionId}/finish
    IC->>IRS: finishAndGenerateReport()
    
    IRS->>ISM: 更新状态为report_generating
    ISM->>MySQL: UPDATE
    
    IRS->>RGS: generateAsync()
    
    RGS->>RGS: 构建报告生成输入
    Note over RGS,RGS: 包含:
    Note over RGS,RGS: • 会话信息
    Note over RGS,RGS: • 所有题目
    Note over RGS,RGS: • 所有回答
    Note over RGS,RGS: • 状态账本
    
    RGS->>AC: callReportGeneration()
    AC->>ReportAgent: 生成报告
    ReportAgent-->>AC: 报告
    AC-->>RGS: 报告
    
    RGS->>IRM: 保存InterviewReport
    IRM->>MySQL: INSERT
    
    RGS->>ISM: 更新状态为completed
    ISM->>MySQL: UPDATE
    
    IRS-->>IC: OK
    IC-->>FE: OK
    
    FE->>FE: 轮询报告
    FE->>IC: GET /interviews/{sessionId}/report
    IC->>IRS: getReport()
    IRS->>IRM: SELECT
    IRM-->>IRS: InterviewReport
    IRS-->>IC: InterviewReportDto
    IC-->>FE: 报告
    
    FE->>User: 展示面试结果
```

## 3. Agent协作与状态转换全景图

```mermaid
stateDiagram-v2
    [*] --> planning: 创建会话
    planning --> in_progress: Planner完成
    in_progress --> report_generating: 决策WRAPUP
    report_generating --> completed: 报告生成完成
    planning --> aborted: Planner失败
    in_progress --> aborted: 系统异常

    note right of planning
        Planner Agent 工作:
        • 分析简历和岗位
        • 生成面试考纲
        • 初始化状态账本
        • 生成第一道题
    end note

    note right of in_progress
        循环答题:
        • Evaluation Agent 评估答案
        • 策略筛选可用策略
        • 状态账本更新
        • QuestionGen Agent 生成下一题
        • RAG 检索增强
    end note

    note right of report_generating
        Report Agent 工作:
        • 综合所有题目和回答
        • 生成雷达图评分
        • 分析薄弱点
        • 生成学习推荐
    end note
```

## 4. 核心循环协作图（答题循环）

```mermaid
flowchart TB
    subgraph 答题循环核心
        Start[开始答题] --> ShowQ[展示当前题]
        ShowQ --> UserAns[用户回答]
        UserAns --> Submit[提交答案]
        
        Submit --> Idempotent{幂等性检查}
        Idempotent --> |已存在| ReturnExisting[返回已存在结果]
        Idempotent --> |不存在| Validate[验证会话/题目]
        
        Validate --> StrategyFilter[策略筛选<br>AvailableStrategyAssembler]
        StrategyFilter --> BuildEval[构建评估输入<br>包含可用策略]
        
        BuildEval --> EvalAgent[Evaluation Agent<br>评估答案]
        EvalAgent --> ValidateDecision{验证决策}
        
        ValidateDecision --> |有效| UseRaw[使用原始决策]
        ValidateDecision --> |无效| Repair[DecisionRepairOrchestrator<br>修复决策]
        
        Repair --> |修复成功| UseRepaired[使用修复后决策]
        Repair --> |修复失败| Fallback[SystemFallbackPlanBuilder<br>系统兜底]
        
        UseRaw --> StateUpdate[状态更新<br>StateLedgerReducer]
        UseRepaired --> StateUpdate
        Fallback --> StateUpdate
        
        StateUpdate --> |更新:| StateItems
        StateItems --> Quota[• 配额管理]
        StateItems --> Covered[• 知识点覆盖]
        StateItems --> Active[• 活动项跟踪]
        StateItems --> Rotation[• 轮换索引]
        
        StateUpdate --> Persist[持久化回答<br>InterviewAttempt]
        Persist --> DecisionType{决策类型}
        
        DecisionType --> |WRAPUP| ReportGen[生成报告<br>Report Agent]
        DecisionType --> |CONTINUE| NextQ[生成下一题]
        
        NextQ --> RagCheck{是否需要RAG}
        RagCheck --> |是| RagRetrieve[RAG检索<br>向量库+重排序]
        RagCheck --> |否| BuildGenInput[构建生成输入]
        
        RagRetrieve --> BuildGenInput
        BuildGenInput --> QGenAgent[QuestionGen Agent<br>流式生成]
        
        QGenAgent --> Cache[Redis缓存<br>SSE断点续传]
        Cache --> Push[SSE推送<br>delta + tts_ready + done]
        Push --> ShowQ
    end

    subgraph RAG协作流程
        RagRetrieve --> RPC[RagPlanCompiler<br>编译检索计划]
        RPC --> RRS[RagRetrievalService<br>执行检索]
        RRS --> VS[向量数据库<br>向量检索]
        VS --> RR[RerankService<br>重排序]
        RR --> BuildContext[构建RAG上下文]
    end

    subgraph 状态数据结构
        StateLedger[state_ledger_json]
        StateLedger --> QuotaState[quota_state<br>配额状态]
        StateLedger --> CoveredPoints[covered_points<br>已覆盖知识点]
        StateLedger --> ActiveItem[active_item_*<br>活动项]
        StateLedger --> RotationIndex[rotation_index<br>轮换索引]
        StateLedger --> AskedTotal[asked_total<br>总题数]
    end
```

## 5. Agent调用与数据流向汇总表

| Agent | 调用时机 | 输入数据 | 输出数据 | 协作组件 |
|-------|---------|---------|---------|---------|
| **Planner Agent** | 创建面试时 | 简历、岗位、历史 | 考纲、首题 | PlannerOrchestrationService, StateLedgerInitService |
| **Evaluation Agent** | 每次提交答案后 | 题目、答案、历史、策略、配额 | 评估决策、下一步计划 | AnswerSubmitService, AvailableStrategyAssembler, DecisionExecutionPlanBuilder |
| **QuestionGen Agent** | 生成首题/下一题时 | 决策计划、RAG上下文、历史 | 题目（流式） | FirstQuestionGenerationService, QuestionStreamService, RagRetrievalService |
| **Report Agent** | 面试结束后 | 所有题目、答案、状态账本 | 面试报告 | ReportGenerationService |

## 6. RAG接入与数据流向

| 环节 | RAG动作 | 输入 | 输出 | 失败处理 |
|-----|---------|------|------|---------|
| **检索计划编译** | RagPlanCompiler.compile() | 决策计划、岗位、经验 | RagRetrievalRequest | 跳过RAG，不影响主流程 |
| **向量检索** | RagRetrievalService.retrieve() | RagRetrievalRequest | 初始检索结果 | 回退到空上下文 |
| **重排序** | RerankService.rerank() | 初始检索结果 | 重排序结果 | 使用初始结果 |
| **上下文构建** | 构建RAG上下文 | 重排序结果 | RagContext（总结+材料+候选） | 使用空上下文 |

## 7. 策略筛选与状态更新关键数据

### 7.1 策略筛选输入输出

| 输入参数 | 说明 | 输出策略 |
|---------|------|---------|
| 当前题目类型 | 当前题目的类型（PRINCIPLE等） | INTRO - 自我介绍（未使用） |
| 是否有项目 | 是否有项目经验 | PRINCIPLE - 原则题（配额未满） |
| 剩余知识域 | 未覆盖的知识域列表 | FOLLOWUP - 深入追问（上题为原则） |
| 配额状态 | 各类题目已用数量 | PROJECT_DEEP_DIVE - 项目深挖（有项目） |
| 经验级别 | 用户经验级别 | DOMAIN_SWITCH - 领域跳转（有剩余域） |
| | | WRAPUP - 结束（配额已满） |

### 7.2 状态更新数据项

| 状态项 | 说明 | 更新时机 |
|-------|------|---------|
| quota_state | 各类题目配额计数 | 每次生成新题后 |
| covered_points | 已覆盖的知识点列表 | 每次评估答案后 |
| covered_domains | 已覆盖的知识域列表 | 每次评估答案后 |
| active_item_key | 当前活动项key | 切换到新项目时 |
| active_item_type | 当前活动项类型 | 切换到新项目时 |
| active_item_name | 当前活动项名称 | 切换到新项目时 |
| rotation_index | 兜底策略轮换索引 | 使用兜底策略后 |
| asked_total | 总题数 | 每次生成新题后 |

## 8. 完整数据持久化流向

```mermaid
flowchart LR
    A[面试配置] --> B[InterviewPreference]
    B --> MySQL
    
    C[创建会话] --> D[InterviewSession<br>status=planning]
    D --> MySQL
    
    E[Planner完成] --> F[更新InterviewSession<br>syllabus_json<br>state_ledger_json<br>status=in_progress]
    E --> G[InterviewQuestion<br>首题]
    F --> MySQL
    G --> MySQL
    
    H[提交答案] --> I[InterviewAttempt]
    H --> J[更新InterviewSession<br>state_ledger_json]
    I --> MySQL
    J --> MySQL
    
    K[生成下一题] --> L[InterviewQuestion]
    K --> M[更新InterviewSession<br>state_ledger_json<br>current_question_no]
    L --> MySQL
    M --> MySQL
    
    N[结束面试] --> O[InterviewReport]
    N --> P[更新InterviewSession<br>status=completed]
    O --> MySQL
    P --> MySQL
    
    Q[SSE流式数据] --> R[Redis<br>缓存]
```

## 9. 缓存与容错机制全景

```mermaid
flowchart TB
    subgraph 缓存层
        Redis_Cache[Redis缓存]
        Redis_Cache --> SSE_Status[sse:gen:{id}:status]
        Redis_Cache --> SSE_Chunks[sse:gen:{id}:chunks]
        Redis_Cache --> SSE_QuestionId[sse:gen:{id}:questionId]
        Redis_Cache --> SSE_TTS[sse:gen:{id}:ttsReady]
        Redis_Cache --> SSE_Done[sse:gen:{id}:donePayload]
        Redis_Cache --> SSE_Error[sse:gen:{id}:errorPayload]
    end

    subgraph 容错层
        Idempotent_Check[幂等性检查]
        Decision_Validation[决策验证]
        Decision_Repair[决策修复<br>DecisionRepairOrchestrator]
        System_Fallback[系统兜底<br>SystemFallbackPlanBuilder]
        Question_Fallback[题目兜底<br>兜底题生成]
        RAG_Fallback[RAG容错<br>空上下文回退]
    end

    subgraph 数据流
        Start1[答题提交] --> Idempotent_Check
        Idempotent_Check --> |已存在| Return_Cached[返回缓存结果]
        Idempotent_Check --> |不存在| Eval_AI[调用Evaluation Agent]
        
        Eval_AI --> Decision_Validation
        Decision_Validation --> |有效| Use_Raw[使用原始决策]
        Decision_Validation --> |无效| Decision_Repair
        
        Decision_Repair --> |修复成功| Use_Repaired[使用修复决策]
        Decision_Repair --> |修复失败| System_Fallback
        
        Use_Raw --> Update_State[更新状态]
        Use_Repaired --> Update_State
        System_Fallback --> Update_State
        
        Start2[题目生成] --> RAG_Check{需要RAG?}
        RAG_Check --> |是| RAG_Retrieve[RAG检索]
        RAG_Retrieve --> RAG_Fallback
        RAG_Fallback --> Build_Input[构建生成输入]
        RAG_Check --> |否| Build_Input
        
        Build_Input --> QGen_AI[调用QuestionGen Agent]
        QGen_AI --> QGen_Error{出错?}
        QGen_Error --> |是| Question_Fallback
        QGen_Error --> |否| Save_Question[保存题目]
        
        Question_Fallback --> Save_Question
    end
```

## 10. 系统协作总结

### 10.1 Agent协作流程
1. **Planner Agent** → 初始化面试、生成考纲、首题
2. **Evaluation Agent** → 评估答案、决定下一步策略
3. **QuestionGen Agent** → 流式生成题目（可配合RAG）
4. **Report Agent** → 生成综合面试报告

### 10.2 RAG接入流程
1. **计划编译** → 根据决策计划确定检索目标
2. **向量检索** → 从向量库检索相关材料
3. **重排序** → 优化检索结果排序
4. **上下文构建** → 整合材料供题目生成使用

### 10.3 策略筛选机制
1. **输入参数** → 题目类型、项目、剩余域、配额、经验
2. **筛选逻辑** → 依次检查各策略可用性
3. **输出策略** → 提供可用策略列表给AI决策

### 10.4 状态更新策略
1. **配额管理** → 跟踪各类题目数量
2. **知识点覆盖** → 记录已覆盖的知识点
3. **活动项跟踪** → 跟踪当前探讨的项目
4. **轮换索引** → 用于兜底策略轮换

### 10.5 缓存与容错
1. **Redis缓存** → SSE流式数据，支持断点续传
2. **幂等性检查** → 防止重复提交
3. **决策修复** → AI决策无效时修复
4. **系统兜底** → 修复失败时使用兜底策略
5. **题目兜底** → AI题目生成失败时使用兜底题
6. **RAG容错** → RAG失败时回退到空上下文
